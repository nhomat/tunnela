export function PageIcon3D({ children }: { children: React.ReactNode }) {
  return (
    <span className="page-icon-3d" aria-hidden="true">
      <span className="page-icon-3d-inner">{children}</span>
    </span>
  );
}
